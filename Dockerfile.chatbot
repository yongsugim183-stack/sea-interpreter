FROM python:3.11-slim

WORKDIR /app

COPY requirements-chatbot.txt .
RUN pip install --no-cache-dir -r requirements-chatbot.txt

COPY chatbot_server.py chatbot.html ./

ENV PORT=8000

EXPOSE 8000

CMD uvicorn chatbot_server:app --host 0.0.0.0 --port ${PORT:-8000}
